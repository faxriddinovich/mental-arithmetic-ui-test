import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mental.arithmetic.app',
  appName: 'Mental Arithmetic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
